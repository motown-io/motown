CREATE TABLE SOURCEENDPOINT(
  ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
  PMSIDENTIFIER VARCHAR(255) NOT NULL,
  SOURCEENDPOINTURL VARCHAR(255) NOT NULL,
  CONSTRAINT UK_KUNNENWIJDANALLES55BJ06OQ3 UNIQUE(PMSIDENTIFIER)
);
