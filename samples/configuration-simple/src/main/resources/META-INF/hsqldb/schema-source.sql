CREATE TABLE DESTINATIONENDPOINT(
  ID BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL PRIMARY KEY,
  PMSIDENTIFIER VARCHAR(255) NOT NULL,
  DESTINATIONENDPOINTURL VARCHAR(255) NOT NULL,
  CONSTRAINT UK_KUNNENWIJDANALLESD5BJ06OQ3 UNIQUE(PMSIDENTIFIER)
);
