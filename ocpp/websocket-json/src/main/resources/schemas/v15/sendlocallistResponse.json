{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "title": "SendLocalListResponse",
    "type": "object",
    "properties": {
        "status": {
            "type": "string",
            "enum": [
                "Accepted",
                "Failed",
                "HashError",
                "NotSupported",
                "VersionMismatch"
            ]
        },
        "hash": {
            "type": "string"
        }
    },
    "required": [
        "status"
    ]
}